on: push

jobs:
  echo-input:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/github-script@v6
        with:
          script: |
               const res = github.rest.repos.listForOrg({org: 'defenseunicorns'})
               console.log(res.data)


                  
            
